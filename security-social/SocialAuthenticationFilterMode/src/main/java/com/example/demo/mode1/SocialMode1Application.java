package com.example.demo.mode1;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = {"com.example.demo.mode1"})
public class SocialMode1Application {
    public static void main(String[] args) {
        SpringApplication.run(SocialMode1Application.class);
    }
}
